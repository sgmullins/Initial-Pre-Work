// I learned that JS has a function called Math.random() that will return a random number between 0-almost 1

function randomFraction() {

    // Only change code below this line.
  
    return Math.random();
  
    // Only change code above this line.
  }