//Else statements can be added after the if statement if you want something else to execute when the if statement is false

function testElse(val) {
    var result = "";
    // Only change code below this line
    
    if (val > 5) {
      return "Bigger than 5";
    } else {
      return "5 or Smaller";
    }
    
    
    // Only change code above this line
    return result;
  }
  
  // Change this value to test
  testElse(4);
  