// I learned that since boolean operators return true or false, you can skip the if statements and just return the result of the comparison

function isLess(a, b) {
    // Fix this code
   
    return a < b;
  }
  
  // Change these values to test
  isLess(10, 15);