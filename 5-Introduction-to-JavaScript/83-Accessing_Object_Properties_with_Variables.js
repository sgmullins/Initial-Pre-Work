// I learned that object properties can be accessed via variables that have previously been assigned a value relating to the object property

// Setup
var testObj = {
    12: "Namath",
    16: "Montana",
    19: "Unitas"
  };
  
  // Only change code below this line;
  
  var playerNumber = 16;       // Change this Line
  var player = testObj[playerNumber];   // Change this Line