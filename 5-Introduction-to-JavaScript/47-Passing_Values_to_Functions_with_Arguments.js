// I learned that when you define a function you can also define parameters for that function which will then accept arguments when the function is called

// Example
function ourFunctionWithArgs(a, b) {
    console.log(a - b);
  }
  ourFunctionWithArgs(10, 5); // Outputs 5
  
  // Only change code below this line.
  
  function functionWithArgs(a, b){
    console.log(a + b);
  };
  
  functionWithArgs(5, 6);